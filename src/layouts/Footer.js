import React from "react";

const Footer = () => {
  return (
    <footer>
      <nav>
        <div>{new Date().getFullYear()} &copy; your copyright</div>
        <div></div>
      </nav>
    </footer>
  );
};

export default Footer;
